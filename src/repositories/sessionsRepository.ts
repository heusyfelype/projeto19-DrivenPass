import { prisma } from "../config/connectionDB.js";


// export async function getSessionById(sessionId: number) {
//     const session = prisma.sessions.findFirst({ where: { id: sessionId } })
//     return session
// }